# LlamaNet - Cloud / Gateway dependencies (MACHAAO deployment)
# Lightweight — no native compilation, no GPU libs
fastapi>=0.68.0,<1.0.0
uvicorn[standard]
pydantic>=2.0.0,<3.0.0
requests>=2.26.0,<3.0.0
aiohttp>=3.8.0,<4.0.0
httptools
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
